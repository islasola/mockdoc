# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-04-03T08:18:00.136Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main
    paths:
      - docs/**
      - apis/**
      - scripts/**
      
env:
  readme_api: ${{secrets.README_API_KEY}}
  figma_api: ${{secrets.FIGMA_API}}
  notion_api: ${{secrets.NOTION_API}}
  aws_ak: ${{secrets.AWS_AK}}
  aws_sk: ${{secrets.AWS_SK}}

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3
        
      - name: Update docs 📚
        run: |
          python3 ./scripts/update_guides.py

      - name: Clean openapi specs 🧹
        run: |
          python3 ./scripts/clean.py

      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi ./apis/clean.json --key=${{ secrets.README_API_KEY }} --id=64368bb63b18090510bad282 --useSpecVersion --update

      - name: Generate restful reference 🚀
        run: |
          mkdir restref
          python3 ./scripts/refgen.py ${{ secrets.README_API_KEY }}

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs ./restref --key=${{ secrets.README_API_KEY }} --version=1.0.0    
