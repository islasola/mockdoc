# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-04-03T07:23:20.172Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - name: Install rdme
        run: |
          npm install -g rdme
      - name: Get Merged Files
        uses: peter-evans/get-pull-request@v3
        id: pr-files
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          pull_request_number: ${{ github.event.pull_request.number }}
      - name: Loop Through Merged Files
        run: |
          for file in ${{ steps.pr-files.outputs.files_modified }}
          do
            echo "Processing merged file: $file"
            # Do something with the merged file here
          done
